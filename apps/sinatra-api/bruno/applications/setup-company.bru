meta {
  name: Setup Parent Company
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/companies
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Parent Co (applications suite)",
    "industry": "Software",
    "location": "Remote"
  }
}

assert {
  res.status: eq 201
  res.body.id: isNumber
  res.body.name: eq "Parent Co (applications suite)"
}

tests {
  test("stash parent companyId for the applications suite", function() {
    bru.setVar("applicationsCompanyId", res.body.id);
  });
}
