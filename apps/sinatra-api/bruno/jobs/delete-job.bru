meta {
  name: Delete Job
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/jobs/{{jobId}}
  body: none
  auth: none
}

assert {
  res.status: eq 204
}

tests {
  test("response body is empty", function() {
    expect(res.body == null || res.body === "").to.be.true;
  });
}
