{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "bundle exec rerun --dir . --pattern '**/*.rb' -- rackup config.ru -p 4567",
    "start": "bundle exec rackup config.ru -p 4567",
    "install:ruby": "bundle install"
  }
}
